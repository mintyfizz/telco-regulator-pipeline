select
  event_id,
  event_fingerprint,
  domain,
  severity,
  event_code,
  event_message,
  detection_rule,
  operator_id,
  service_segment,
  report_period,
  region_code,
  source_table,
  source_id,
  bronze_id,
  metric_name,
  observed_value,
  expected_min,
  expected_max,
  status,
  detected_at,
  detected_by_run_id,
  metadata
from {{ source('silver', 'data_quality_events') }}

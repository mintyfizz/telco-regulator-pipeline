name: telco_regulator_pipeline
version: 0.9.0
config-version: 2

profile: telco_regulator_pipeline

model-paths: ["models"]
test-paths: ["tests"]

models:
  telco_regulator_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: view
